/// Engine → renderer contract: immutable per-tick snapshots the Flame layer
/// reads to draw the game (M1-021).
///
/// Nothing in this barrel imports `package:flutter` or `package:flame`.
library;

export 'render_state.dart';
