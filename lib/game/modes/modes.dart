/// Selectable game modes (M2-019..021).
///
/// A `GameMode` is pure config the game layer turns into a `Simulation`
/// (target score + optional time limit), including the timer-expiry contract
/// shared by timed modes.
library;

export 'game_mode.dart';
